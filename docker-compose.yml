version: '3' 

services:
  jupyter:
    image: jupyter/datascience-notebook 
    command: ["start-notebook.sh", "--NotebookApp.token=''"]
    volumes:
      - "${DATA:-./data}:/home/jovyan" 
    ports:
      - "${WEB:-8888}:8888"
